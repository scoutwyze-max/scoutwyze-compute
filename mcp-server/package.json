{
  "name": "scoutwyze-compute-mcp",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "MCP server wrapper for ScoutWyze Compute — ranks current RunPod GPU offers by price and freshness. Stateless pass-through: holds no private keys, never executes payments itself.",
  "bin": {
    "scoutwyze-compute-mcp": "./dist/index.js"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "start": "node dist/index.js",
    "dev": "tsx src/index.ts"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.30.1",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "@types/node": "^20.14.10",
    "tsx": "^4.16.2",
    "typescript": "^5.5.3"
  },
  "engines": {
    "node": ">=18"
  }
}
